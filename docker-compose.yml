version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: it_freelance_mysql
    environment:
      MYSQL_DATABASE: it_freelance_db
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_ROOT_HOST: "%"
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - it_freelance__mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot --silent"]
      interval: 5s
      timeout: 5s
      retries: 30

  backend:
    build:
      context: ./it-freelance-back
      dockerfile: Dockerfile
    container_name: it_freelance_back
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8000

      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: it_freelance_db
      DB_USERNAME: root
      DB_PASSWORD: ""

      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./it-freelance-front
      dockerfile: Dockerfile
    container_name: it_freelance_front
    depends_on:
      - backend
    environment:
      CHOKIDAR_USEPOLLING: "true"
      HOST: "0.0.0.0"
      # ako ti React čita base url iz env:
      REACT_APP_API_BASE_URL: "http://localhost:8000/api"
    ports:
      - "3000:3000"

volumes:
  it_freelance__mysql_data:
